% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{eavs_download}
\alias{eavs_download}
\title{Download published EAVS data files}
\usage{
eavs_download(
  years = NULL,
  survey = "eavs",
  format = "csv",
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{years}{Survey years to download. \code{NULL} (the default) fetches every
year available for the chosen \code{survey} and \code{format}.}

\item{survey}{Which survey to fetch: \code{"eavs"} (default) or \code{"policy"} for
the Policy Survey (available from 2018 on).}

\item{format}{File format: \code{"csv"} (default) or \code{"xlsx"}.}

\item{overwrite}{Re-download even when a verified copy is already cached.}

\item{quiet}{Suppress progress messages.}
}
\value{
A tibble with one row per file and columns \code{year}, \code{survey},
\code{format}, \code{path} (the cached file), and \code{status}, one of \code{"reused"} (a
verified copy was already present) or \code{"downloaded"}.
}
\description{
Fetches EAVS data file(s) for the requested survey year(s) into the local
cache (see \code{\link[=eavs_cache_dir]{eavs_cache_dir()}}). Files come from the tidyeavs data mirror
when one is recorded and otherwise directly from the U.S. Election
Assistance Commission. A file already in the cache is reused once its
checksum matches, so a given file is downloaded only once.
}
\examples{
\dontrun{
# Fetch the 2022 and 2024 EAVS as CSV:
eavs_download(c(2022, 2024))
}
}
