% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{eavs_jurisdictions}
\alias{eavs_jurisdictions}
\title{EAVS jurisdictions, with quirk flags}
\format{
A tibble with one row per jurisdiction-year and the columns:
\describe{
\item{year}{Survey year.}
\item{fips_code}{The jurisdiction code exactly as published.}
\item{fips10}{\code{fips_code} normalized to 10 digits (restoring lost leading
zeros), or \code{NA} for codes that are not FIPS-style: Wisconsin's serials
and Maine's statewide row.}
\item{jurisdiction_name}{Jurisdiction name as published.}
\item{state_abbr}{Two-letter state or territory abbreviation as
published.}
\item{state_name, state_fips}{The standard name and two-digit ANSI FIPS
for \code{state_abbr}.}
\item{county_fips}{Five-digit county (or county-equivalent) FIPS, where
the published code embeds one; \code{NA} where it does not (Wisconsin,
Alaska, territories, place-coded Illinois city boards, a few Maine
townships).}
\item{type}{Structural type: \code{"county"} (county or county-equivalent,
including parishes, independent cities, and the District of Columbia),
\code{"municipality"} (sub-county: New England towns, Wisconsin
municipalities, Illinois city election boards), \code{"statewide"} (Alaska's
single row and Maine's UOCAVA row), or \code{"territory"}.}
\item{nongeo_code}{\code{TRUE} for Wisconsin's non-geographic serial codes.}
\item{code_padded}{\code{TRUE} where the published code lost a leading zero
and \code{fips10} restores it.}
\item{shared_code}{\code{TRUE} where two published rows share one code that
year (three Wisconsin town/village pairs).}
\item{note}{Description of the row's quirk, or \code{NA}.}
}
}
\source{
Built from the published EAVS files. See
\code{data-raw/jurisdictions.R}, which records the structural facts and the
years they were verified against.
}
\usage{
eavs_jurisdictions
}
\description{
One row per jurisdiction per EAVS year, exactly as the published files list
them: identifiers, a normalized FIPS code, the county FIPS where the
published code embeds one, a structural type, and flags for the quirks that
matter when joining or aggregating. Every published row is kept—including
statewide pseudo-rows and duplicated codes—and quirks are flagged in
\code{note}, never corrected.
}
\details{
The quirks worth knowing: Wisconsin's ~1,850 codes are non-geographic
serials (the county is only in the name, and a few serials are shared by a
town/village pair); Maine files a statewide row carrying only its UOCAVA
totals; Alaska and the territories file one row each; Kalawao County HI
appears but is administered by Maui County; and two 2024 California codes
lost a leading zero, which \code{fips10} restores. Connecticut's embedded county
codes are the pre-2022 Census counties, not today's planning regions.
Covers the EAVS only, not the Policy Survey.
}
\keyword{datasets}
