% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load.R
\name{eavs_load}
\alias{eavs_load}
\title{Load a tidy, harmonized EAVS panel}
\usage{
eavs_load(years, survey = "eavs", format = "csv", quiet = FALSE)
}
\arguments{
\item{years}{Survey years to load.}

\item{survey}{\code{"eavs"} (default) or \code{"policy"}.}

\item{format}{\code{"csv"} (default) or \code{"xlsx"}.}

\item{quiet}{Suppress progress messages.}
}
\value{
A tibble with one row per jurisdiction per year; identifier and
concept columns named consistently across years (see \link{eavs_dictionary}),
plus a \code{year} column.
}
\description{
The one-step entry point. Downloads the requested survey years, decodes
their missing-value codes, and harmonizes variable names so the years stack
into a single jurisdiction-year panel. It is the same as running
\code{\link[=eavs_read]{eavs_read()}}, \code{\link[=eavs_recode_missing]{eavs_recode_missing()}}, and \code{\link[=eavs_harmonize]{eavs_harmonize()}} for each year
and binding the results.
}
\details{
Loading more than one year prints a one-line reminder to run \code{\link[=eavs_flags]{eavs_flags()}}
before publishing, because comparing cycles is where the survey's reporting
changes do their damage and a panel gives no sign of them on its own. \code{quiet}
silences it.
}
\examples{
\dontrun{
panel <- eavs_load(c(2020, 2022, 2024))
}
}
