% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{eavs_join}
\alias{eavs_join}
\title{Attach a table to an EAVS frame without fanning out or dropping rows}
\usage{
eavs_join(
  x,
  y = eavs_jurisdictions,
  by = NULL,
  multiple = c("error", "all"),
  unmatched = c("inform", "warn", "error", "ignore")
)
}
\arguments{
\item{x}{The frame to keep every row of: a panel from \code{\link[=eavs_load]{eavs_load()}}, one
harmonized year, or any frame carrying the join columns.}

\item{y}{The frame to attach. Defaults to \link{eavs_jurisdictions}, the common
case of adding structural type, county FIPS, and quirk flags to a panel.}

\item{by}{Character vector of columns to join on. Defaults to whichever of
\code{"year"} and \code{"fips_code"} are in both frames.}

\item{multiple}{What to do when a key matches several rows in \code{y} and so fans
out \code{x}. \code{"error"} (default) stops and names the shared keys; \code{"all"}
performs the expansion.}

\item{unmatched}{What to do with rows of \code{x} that match nothing in \code{y}.
\code{"inform"} (default) reports the count, \code{"warn"} raises a warning, \code{"error"}
stops, and \code{"ignore"} says nothing. The rows are kept either way, with \code{NA}
in \code{y}'s columns.}
}
\value{
\code{x} with \code{y}'s new columns attached, one row per row of \code{x} unless
\code{multiple = "all"} expands a shared code.
}
\description{
A left join that keeps every row of \code{x} and adds columns from \code{y}, guarding
the two ways a join on EAVS identifiers goes wrong on its own. A code shared
by two published rows matches both and multiplies \code{x}'s rows, inflating any
sum taken afterward; a row with no match in \code{y} disappears. Base \code{merge()}
does both silently and pandas does the first silently, so this reports each
before it reaches a total.
}
\details{
The fan-out is Wisconsin's: three town/village pairs share one serial code
(\code{82575} and \code{84275} in 2020, \code{31550} in 2022), so joining a panel to
\link{eavs_jurisdictions} on \code{fips_code} alone turns four rows into eight and
overstates the state's 2020 \code{partic_total} by 0.35\%. \code{eavs_join()} stops and
names the shared codes; adding \code{jurisdiction_name} to \code{by} matches the pairs
exactly, since the town and village differ only in name. The drop is Maine's:
its UOCAVA totals sit in a statewide row carrying no county FIPS, so a
county-keyed join loses them. \code{eavs_join()} counts the unmatched rows rather
than letting them vanish.

Columns already in \code{x} are kept from \code{x}; \code{eavs_join()} adds only \code{y}'s new
columns, so attaching \link{eavs_jurisdictions} to a panel brings in the county
FIPS, structural type, and quirk flags without duplicating the identifiers
the panel already carries.

Nothing is corrected here: a shared code and a statewide row are how the
jurisdictions reported, and the join reports them rather than resolving them.
}
\examples{
\dontrun{
panel <- eavs_load(c(2020, 2022))

# Attach county FIPS, type, and quirk flags. Stops on Wisconsin's shared
# codes rather than fanning them out.
eavs_join(panel, by = c("year", "fips_code", "jurisdiction_name"))
}
}
\seealso{
\link{eavs_jurisdictions} for the quirk flags this join surfaces,
\code{\link[=eavs_aggregate]{eavs_aggregate()}} and \code{\link[=eavs_rate]{eavs_rate()}} for rollups that handle these rows
without a join you write yourself.
}
