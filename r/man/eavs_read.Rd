% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{eavs_read}
\alias{eavs_read}
\title{Read an EAVS data file}
\usage{
eavs_read(
  year,
  survey = "eavs",
  format = "csv",
  download = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{year}{A single survey year.}

\item{survey}{\code{"eavs"} (default) or \code{"policy"} for the Policy Survey.}

\item{format}{\code{"csv"} (default) or \code{"xlsx"}.}

\item{download}{Whether to download the file if it is not already cached.}

\item{quiet}{Suppress progress messages.}
}
\value{
A tibble of the data as published, with \code{year} and \code{survey} columns
added at the front. Every other column is character.
}
\description{
Reads a single survey year of EAVS data from the cache, downloading it first
if necessary. The data are returned as published, with every column as
character. Reading everything as text preserves FIPS codes with leading
zeros and the survey's missing-value codes without guessing types.
}
\details{
Use \code{\link[=eavs_recode_missing]{eavs_recode_missing()}} to turn item columns into numbers, or
\code{\link[=eavs_load]{eavs_load()}} for a cleaned, harmonized panel across years.
}
\examples{
\dontrun{
raw_2024 <- eavs_read(2024)
}
}
