% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{eavs_cache_dir}
\alias{eavs_cache_dir}
\title{Where tidyeavs stores downloaded data}
\usage{
eavs_cache_dir(create = TRUE)
}
\arguments{
\item{create}{Whether to create the directory if it does not yet exist.
Defaults to \code{TRUE}.}
}
\value{
The cache directory path, as a string.
}
\description{
EAVS data files are downloaded once and kept in a local cache, so that
later calls reuse the file instead of fetching it again. This returns the
cache directory.
}
\details{
The location is resolved in this order:
\enumerate{
\item the \code{tidyeavs.cache_dir} option, if set;
\item the \code{TIDYEAVS_CACHE_DIR} environment variable, if set;
\item \code{tools::R_user_dir("tidyeavs", "cache")}, the standard per-user cache
directory for R packages.
}

To use a project-local cache for one session, set the option before
downloading, e.g. \code{options(tidyeavs.cache_dir = "data/eavs")}.
}
\examples{
# Where files will be stored (without creating the directory):
eavs_cache_dir(create = FALSE)
}
