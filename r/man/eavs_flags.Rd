% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flags.R
\name{eavs_flags}
\alias{eavs_flags}
\title{Flag internal inconsistencies in an EAVS panel}
\usage{
eavs_flags(
  data,
  checks = NULL,
  swing_factor = 10,
  swing_floor = 100,
  anomalies = NULL,
  jurisdictions = NULL
)
}
\arguments{
\item{data}{A harmonized panel from \code{\link[=eavs_load]{eavs_load()}}. Needs \code{year} and, for
swing checks, more than one year.}

\item{checks}{The checks to run. Defaults to the bundled \link{eavs_checks}; pass a
subset or your own to change them. Set to \code{NULL} to skip sum checks.}

\item{swing_factor}{Flag a value that is more than this many times, or less
than this fraction of, the same jurisdiction's previous cycle. \code{NULL} skips
swing checks. Defaults to \code{10}.}

\item{swing_floor}{Ignore swings where both cycles are below this value, so
that a count going from 1 to 20 does not dominate the output. Defaults
to \code{100}.}

\item{anomalies}{The known anomalies to surface. Defaults to the bundled
\link{eavs_known_anomalies}; pass your own or set to \code{NULL} to skip them.}

\item{jurisdictions}{The jurisdiction table used to line jurisdictions up
across years for the swing checks. Defaults to the bundled
\link{eavs_jurisdictions}.}
}
\value{
A tibble with one row per flag: \code{year}, \code{fips_code}, \code{state_abbr},
\code{jurisdiction_name}, \code{check}, \code{kind} (\code{"sum"}, \code{"swing"}, or
\code{"known_anomaly"}), \code{concept},
\code{observed}, \code{threshold}, \code{excess}, \code{n_parts_reported}, \code{state_share}, and
\code{note}. Zero rows if nothing is flagged.
}
\description{
Runs a set of arithmetic checks over a harmonized panel and returns one row
per flagged observation. Three kinds: subparts summing past a total (see
\link{eavs_checks}), a value swinging by more than \code{swing_factor} against the same
jurisdiction's previous cycle, and the reporting anomalies recorded in
\link{eavs_known_anomalies}, which arithmetic cannot catch because the numbers
reconcile internally.
}
\section{These are flags, not errors}{

A flag says two reported numbers do not reconcile arithmetically. It does not
say either is wrong. Thousands of independent jurisdictions keep records in
different ways, and most flags have ordinary explanations: ballots transmitted
before the reporting window opened, a jurisdiction that treats "returned" as
"returned and accepted" so rejections sit outside it, mode categories that
overlap, registration forms counted with or without duplicates. Each check's
\code{note} in \link{eavs_checks} spells out the usual explanations for that check.

Nothing is changed. tidyeavs does not alter reported values, and this function
only reports; deciding what a flag means for your analysis is yours. The EAC
publishes its own validation rules and revises datasets as corrections come
in, so a flag here may already be known upstream.
}

\section{Reading the output}{

For a sum check, \code{observed} is the sum of the check's \code{parts}, \code{threshold} is
the \code{total} it was compared against, and \code{excess} is the difference in
ballots (or forms, or whatever the concept counts). For a swing, \code{observed} is
this cycle's value, \code{threshold} is the previous cycle's, and \code{excess} is how
far the fold-change ran past \code{swing_factor}. The two are on different scales,
so the result is sorted within \code{kind} rather than across it; filter to one
kind before sorting yourself.

\code{n_parts_reported} says how many of the check's parts had a value. A partial
sum that already exceeds the total is still a genuine flag, since the absent
parts could only add to it.

\code{state_share} is roughly how much of its state's total for that concept the
flagged jurisdiction holds, and it is the column to sort by when the question
is whether a flag could move a number you plan to publish. A discrepancy in a
jurisdiction holding 0.2\% of the state's mail ballots cannot; one holding 40\%
can. \code{excess} will not tell you this, because it is measured on the concept's
own scale: a large county's small discrepancy outranks a small county's
complete one.

The numerator is the larger of \code{observed} and \code{threshold}, since the smaller
one is often the reason the flag fired. Utah's Washington County reports 943
mail ballots returned in 2022 against 65,664 counted, so taking the reported
total would call it 0.09\% of the state when the county cast 6.5\% of Utah's
ballots. The share can therefore run above 1. That happens on a sum check
where the suspect total is most of what the state reported (the Virgin Islands
file as a single jurisdiction), and on a swing where the larger number belongs
to the previous cycle while the denominator belongs to this one.

\code{state_share} measures size, not severity. What a flag means for your analysis
is still yours to decide.

For a known anomaly there is no comparison, only a documented reason to read
the value with the \code{note} in hand, so \code{observed} is the reported value and
\code{threshold}, \code{excess}, and \code{n_parts_reported} are \code{NA}.
}

\examples{
\dontrun{
panel <- eavs_load(c(2022, 2024))
flags <- eavs_flags(panel)

# Largest discrepancies first:
flags[order(-flags$excess), ]

# Or the ones big enough to move a state number:
sums <- flags[flags$kind == "sum", ]
head(sums[order(-sums$state_share), c("state_abbr", "jurisdiction_name",
                                      "check", "state_share")])
}
}
\seealso{
\link{eavs_checks} for the check definitions, \code{\link[=eavs_aggregate]{eavs_aggregate()}} for
coverage-aware rollups.
}
