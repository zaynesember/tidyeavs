% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recode.R
\name{eavs_recode_missing}
\alias{eavs_recode_missing}
\title{Decode EAVS missing-value codes}
\usage{
eavs_recode_missing(data)
}
\arguments{
\item{data}{A data frame from \code{\link[=eavs_read]{eavs_read()}}, or any table of EAVS columns.}
}
\value{
\code{data} with item columns converted to numeric and sentinel values
set to \code{NA}. Other columns are unchanged.
}
\description{
EAVS marks non-substantive responses with negative sentinel codes. From
2018 on these are \code{-88} ("does not apply"), \code{-99} ("data not available"),
and \code{-77} ("valid skip"); in 2016 and earlier they are \code{-888888} and
\code{-999999}. This finds the numeric item columns in a raw EAVS table (see
\code{\link[=eavs_read]{eavs_read()}}), converts them to numbers, and sets sentinel values to \code{NA}.
}
\details{
Identifier and text columns—FIPS codes, jurisdiction and state names,
comment and write-in fields—are left untouched. A column is treated as a
numeric item when, ignoring blanks and text placeholders, all of its values
are numbers. Because every EAVS item is a count or a rate, any negative
value in an item column is read as missing; recognized sentinels keep their
specific meaning, which \code{\link[=eavs_missing_status]{eavs_missing_status()}} reports.
}
\examples{
df <- data.frame(
  FIPSCode = c("01001", "01003"),
  A1a = c("1200", "-99"),
  A3a = c("15", "-88")
)
eavs_recode_missing(df)
}
\seealso{
\code{\link[=eavs_missing_status]{eavs_missing_status()}} to see \emph{why} each value is missing.
}
