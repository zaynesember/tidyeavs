% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize.R
\name{eavs_harmonize}
\alias{eavs_harmonize}
\title{Harmonize an EAVS year to stable concept names}
\usage{
eavs_harmonize(data, year = NULL, keep_unmatched = FALSE, dictionary = NULL)
}
\arguments{
\item{data}{A single survey year of EAVS data, typically from
\code{\link[=eavs_recode_missing]{eavs_recode_missing()}} (so item columns are numeric). A \code{year} column, as
added by \code{\link[=eavs_read]{eavs_read()}}, is used to pick the year.}

\item{year}{The survey year, if \code{data} has no \code{year} column.}

\item{keep_unmatched}{Keep columns that are not in the dictionary, under
their original names. Defaults to \code{FALSE}.}

\item{dictionary}{The dictionary to harmonize against. Defaults to the
bundled \link{eavs_dictionary}; pass your own to use an extended crosswalk.}
}
\value{
A tibble with identifier and concept columns named consistently
across years, plus \code{year}.
}
\description{
Renames a single survey year's raw variables to the concept names shared
across years, so that different years line up. For example, mail ballots
rejected is \code{C4a} in 2020 but \code{C9a} in 2024; both become \code{mail_rejected}.
This is the step that makes years comparable, and it is what turns EAVS's
silent renumbering from a trap into a non-issue.
}
\details{
Only the curated concepts in \link{eavs_dictionary} are renamed. By default other
columns are dropped; set \code{keep_unmatched = TRUE} to keep them under their
original names.
}
\seealso{
\code{\link[=eavs_load]{eavs_load()}} to download, recode, and harmonize in one step.
}
