% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dictionary.R
\name{eavs_items}
\alias{eavs_items}
\title{Look up EAVS variables and concepts}
\usage{
eavs_items(query = NULL, year = NULL, section = NULL, dictionary = NULL)
}
\arguments{
\item{query}{A string matched (case-insensitively) against concept names,
labels, raw codes, and EPI names. An exact concept name wins outright, so
\code{"mail_rejected"} returns that concept rather than it plus the sixteen
\verb{mail_rejected_*} reasons; search \code{"mail_rejected_"} for the family.
\code{NULL} (default) returns everything, subject to \code{year} and \code{section}.}

\item{year}{Restrict to one or more survey years.}

\item{section}{Restrict to one or more survey sections (\code{"A"} through \code{"F"},
or \code{"id"} for identifier columns).}

\item{dictionary}{The dictionary to search. Defaults to the bundled
\link{eavs_dictionary}; pass your own to search an extended crosswalk.}
}
\value{
A tibble of matching dictionary rows (see \link{eavs_dictionary}).
}
\description{
Searches the tidyeavs dictionary, which maps each survey year's raw variable
codes to stable concept names. Use it to answer "what is \code{C9a}?", to find
the code for a concept in a given year, or to see which years collected an
item.
}
\examples{
# What does C9a measure, and is it C9a in every year?
eavs_items("C9a")

# All mail-ballot concepts:
eavs_items(section = "C")

# The code for mail ballots rejected, by year:
eavs_items("mail_rejected")
}
